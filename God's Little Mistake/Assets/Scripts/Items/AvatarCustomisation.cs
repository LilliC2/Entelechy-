using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AvatarCustomisation : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //turn game into inventory state (pause everything)
        //instantiate item on canvas
        //item track mouse
        //check for mouse click
            //check if in right zone
            //check if other piece is there
            //if all clear place piece
            //instantiate item in scene at same spot (screentoworldpoint?)
            //make sure item has correct sort order
            //destroy item on canvas
            //activate item in attak database
    }
}
